name: "steadybit/definitions/policies/level-e"
description: "Challenges for redundancy."
type: policy
tasks:
  - name: "steadybit/definitions/experiments/faultless-redundancy-container"
    version: 0.1.2
    forEach:
      iterables: [ container ]
      define:
        containerName: "{{container.name}}"
  - name: "steadybit/definitions/weak-spots/k8s-readiness-probe"
    version: 0.1.2
    forEach:
      iterables: [ container ]
      define:
        containerName: "{{container.name}}"
  - name: "steadybit/definitions/weak-spots/k8s-single-replica"
    version: 0.1.2
    forEach:
      iterables: [ container ]
      define:
        containerName: "{{container.name}}"